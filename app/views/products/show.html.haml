%h1 Product Detail
.btn.btn-default.btn-sm=link_to 'Edit', edit_product_url 
.btn.btn-default.btn-sm=link_to 'Delete', @product, confirm: 'Are you sure?', method: :delete

%h2.name= @product.name       
%p.description= @product.description
%p.price= @product.price      

=link_to 'All Products', products_url 